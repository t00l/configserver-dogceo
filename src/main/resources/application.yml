server:
  port: ${PORT:8081}
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/t00l/breed-repository-property
  profiles:
    active: dev